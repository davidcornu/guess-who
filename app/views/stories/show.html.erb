<h1><%= @story.body %></h1>

This story was submitted by a <%= @story.age %> year old <%= @story.gender %>.

<table>
  <% @story.vote_counts.each do |gender, count| %>
    <tr>
      <td><%= gender.capitalize %></td>
      <td><%= count %></td>
    </tr>
  <% end %>
</table>

<%= link_to "See another story", root_path %>
<%= link_to "Create your own story", new_story_path %>
